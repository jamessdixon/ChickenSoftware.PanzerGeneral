//Create a hex with an image

    //let createImage path =
    //    let image = new Image()
    //    image.Source <- ImageSource.FromResource(path)
    //    image

    //let createRectangle i =
    //    let x = 50.0 
    //    let y = 50.0 * float (i + 1)
    //    let height = 50.0 
    //    let width = 100.0 
    //    let rectangle = new Rectangle(x,y,width,height)
    //    rectangle

    //let populateImage =
        //let layout = new AbsoluteLayout()
        //layout.HeightRequest <- 5000.0
        //layout.WidthRequest <- 5000.0
        //[0..100]
        //|> Seq.iter(fun i -> layout.Children.Add(createImage "tacmap_dry_0_4", createRectangle i))
        //let scrollView = new ScrollView()
        //scrollView.Content <- layout
        //base.Content <- scrollView